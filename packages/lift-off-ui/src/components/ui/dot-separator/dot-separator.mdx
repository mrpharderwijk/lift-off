import { Canvas, Meta, Source, ArgTypes } from '@storybook/blocks';

import * as DotSeparatorStories from './dot-separator.stories';

<Meta of={DotSeparatorStories} />

# DotSeparator

A simple UI component that displays a visual dot separator. Commonly used to visually separate items inline, such as metadata, links, or tags.

<Canvas of={DotSeparatorStories.Default} />

<ArgTypes of={DotSeparatorStories} />
