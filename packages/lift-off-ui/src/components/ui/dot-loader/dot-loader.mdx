import { Canvas, Meta, Source, ArgTypes } from '@storybook/blocks';

import * as DotLoaderStories from './dot-loader.stories';

<Meta of={DotLoaderStories} />

# DotLoader

A `DotLoader` is a simple loading indicator component that displays animated dots. It can be used to indicate loading or ongoing background processes in your UI.

<Canvas of={DotLoaderStories.Small} />

<Canvas of={DotLoaderStories.Medium} />

<Canvas of={DotLoaderStories.Large} />

<ArgTypes of={DotLoaderStories} />
