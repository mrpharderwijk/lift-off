import { Canvas, Meta, Source, ArgTypes } from '@storybook/blocks';

import * as RadioGroupStories from './radio-group.stories';
import { RadioGroup } from './radio-group';

<Meta of={RadioGroupStories} />

# Radio Group

A `Radio Group` is a component that wraps `RadioGroupTile` or `RadioGroupItem` components and allows you to create radio group inputs.

<Canvas of={RadioGroupStories.RadioGroupWithRadioGroupTiles} />

<Canvas of={RadioGroupStories.RadioGroupWithRadioGroupItems} />

<ArgTypes of={RadioGroupStories} />
